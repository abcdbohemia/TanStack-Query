[build]
    command = "npm run build"
    publish = "dist"
    
[[redirects]]
    from = "/*"                                                     
    to = "/index.html"
    status = 200
#This is a test comment to see if Netlify recognizes the update
